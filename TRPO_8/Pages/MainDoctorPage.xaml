<Page x:Class="TRPO_8.Pages.MainDoctorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:converters="clr-namespace:TRPO_8.Convertes"
      xmlns:local="clr-namespace:TRPO_8.Pages"
      mc:Ignorable="d" 
      Background="{DynamicResource MaterialDesignBackground}"
      d:DesignHeight="450" d:DesignWidth="1000"
      Title="MainDoctorPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel>
            <Label Style="{StaticResource LabelStyleTitul}" Content="Кабинет"/>
        </StackPanel>


        <StackPanel Grid.Row="1" Style="{DynamicResource StackPanelOrientationHorizontalTop}">
            <StackPanel>
                <Label Style="{StaticResource LabelStyleMainPage}" Content="Идентификатор" />
                <Label Style="{StaticResource LabelStyleMainPage}" Content="Фамилия"/>
                <Label Style="{StaticResource LabelStyleMainPage}" Content="Имя"/>
                <Label Style="{StaticResource LabelStyleMainPage}" Content="Отчество"/>
                <Label Style="{StaticResource LabelStyleMainPage}" Content="Профессия"/>
            </StackPanel>

            <StackPanel>
                <TextBox Text="{Binding CurrentDoctor.IDDoctor}" Style="{StaticResource RoundedTextBoxStyleMainPage}"/>
                <TextBox Text="{Binding CurrentDoctor.NameDoctor}" Style="{StaticResource RoundedTextBoxStyleMainPage}"/>
                <TextBox Text="{Binding CurrentDoctor.LastNameDoctor}" Style="{StaticResource RoundedTextBoxStyleMainPage}"/>
                <TextBox Text="{Binding CurrentDoctor.MiddleNameDoctor}" Style="{StaticResource RoundedTextBoxStyleMainPage}"/>
                <TextBox Text="{Binding CurrentDoctor.SpecialisationDoctor}" Style="{StaticResource RoundedTextBoxStyleMainPage}"/>
            </StackPanel>
        </StackPanel>


     
        <StackPanel Grid.Row="0" Grid.Column="1"  Style="{DynamicResource StackPanelOrientationHorizontal}">
            <Button Style="{StaticResource ButtonStyleMainPage}" Content="Добавить" Click="CreatNewPatint_Click"/>
            <Button Style="{StaticResource ButtonStyleMainPage}" Content="Начать приём" Click="StartAppointment_Click"/>
            <Button Style="{StaticResource ButtonStyleMainPage}" Content="Изменить" Click="ChangeInfo_Click"/>
            <Button Style="{StaticResource ButtonStyleMainPage}" Content="Удалить" Click="DeleteInfo_Click"/>
        </StackPanel>

        <ListView Grid.Row="1" Grid.Column="1" Style="{StaticResource ModernListViewStyle}"  ItemsSource="{Binding Patients}" SelectedItem="{Binding PatientSelected, Mode=TwoWay}" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <StackPanel Style="{DynamicResource StackPanelOrientationHorizontalList}">
                            <TextBlock Text="ID: "/>
                            <TextBlock Text="{Binding IDPatient}"/>
                        </StackPanel>
                        <StackPanel Style="{DynamicResource StackPanelOrientationHorizontalList}">
                            <TextBlock Text="ФИО: "/>
                            <TextBlock Text="{Binding FullName}"/>
                        </StackPanel>
                        <StackPanel Style="{DynamicResource StackPanelOrientationHorizontalList}">
                            <TextBlock Text="Телефон: "/>
                            <TextBlock Text="{Binding PhonePatient}"/>
                        </StackPanel>
                        <StackPanel Style="{DynamicResource StackPanelOrientationHorizontalList}">
                            <TextBlock Text="День рождения: "/>
                            <TextBlock Text="{Binding BirthdayPatient, StringFormat={}{0:dd.MM.yyyy}}"/>
                        </StackPanel>
                        <StackPanel Style="{DynamicResource StackPanelOrientationHorizontalList}">
                            <TextBlock Text="Возраст: "/>
                            <TextBlock Text="{Binding BirthdayPatient, Converter={StaticResource AgeConverter}}"/>
                        </StackPanel>
                        <StackPanel Style="{DynamicResource StackPanelOrientationHorizontalList}">
                            <TextBlock Text="Статус: "/>
                            <TextBlock Text="{Binding BirthdayPatient, Converter={StaticResource AdultStatusConverter}}"/>
                        </StackPanel>
                        <StackPanel Style="{DynamicResource StackPanelOrientationHorizontalList}">
                            <TextBlock Text="История приёмов: "/>
                            <TextBlock Text="{Binding AppointmentStories.Count}"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Page>
