<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Style TargetType="StackPanel" x:Key="StackPanelStyle">
        <Setter Property="Orientation" Value="Vertical"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="Margin" Value="30"/>
    </Style>


    <Style TargetType="StackPanel" x:Key="StackPanelOrientationHorizontal">
        <Setter Property="Orientation" Value="Horizontal"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>

    <Style TargetType="StackPanel" x:Key="StackPanelOrientationHorizontalRight">
        <Setter Property="Orientation" Value="Horizontal"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Right"/>
    </Style>

    <Style TargetType="StackPanel" x:Key="StackPanelOrientationHorizontalTop" BasedOn="{StaticResource StackPanelOrientationHorizontal}">
        <Setter Property="VerticalAlignment" Value="Top"/>
    </Style>
    <Style TargetType="StackPanel" x:Key="StackPanelOrientationHorizontalButtom" BasedOn="{StaticResource StackPanelOrientationHorizontal}">
        <Setter Property="VerticalAlignment" Value="Bottom"/>
    </Style>

    <Style TargetType="StackPanel" x:Key="StackPanelOrientationHorizontalList">
        <Setter Property="Orientation" Value="Horizontal"/>
    </Style>

</ResourceDictionary>